# =============================================================================
# worker-deployment.yaml — 백그라운드 에이전트(worker) Deployment
# =============================================================================
# worker 는 웹 요청을 받지 않는다(= Service 없음, 포트 없음).
# Redis 큐를 무한히 꺼내(BRPOP) LLM 추천을 생성하는 루프를 돈다.
# stateless 이므로 replicas 를 늘리면 여러 Pod 가 큐를 나눠 처리한다.
#
# probe 주의: HTTP 포트가 없으므로 httpGet liveness 를 쓸 수 없다.
#   → 프로세스가 살아있는지 정도만 exec 로 확인(파이썬 프로세스 존재 체크).
#   더 정교하게 하려면 worker 가 주기적으로 갱신하는 "heartbeat 파일"을 두고
#   그 파일의 갱신 시각을 검사하는 방식으로 발전시킬 수 있다.
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "contest-helper.fullname" . }}-worker
  labels:
    {{- include "contest-helper.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  replicas: {{ .Values.replicaCount.worker }}
  # 작은 클러스터라 롤링 업데이트에 필요한 서지(surge) 여유 리소스가 없다.
  # Recreate 로 기존 파드를 먼저 내리고 새 파드를 띄운다(배포 중 잠깐 다운타임 발생).
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "contest-helper.selectorLabels" (dict "ctx" . "component" "worker") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "contest-helper.selectorLabels" (dict "ctx" . "component" "worker") | nindent 8 }}
    spec:
      containers:
        - name: worker
          image: "{{ .Values.image.worker.repository }}:{{ .Values.image.worker.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          workingDir: /app/services/worker
          # 큐 소비 루프 기동(포트 없음).
          command: ["uv", "run", "--no-dev", "--package", "contest-helper-worker", "python", "-m", "worker.main"]
          envFrom:
            - configMapRef:
                name: {{ include "contest-helper.configmapName" . }}
            - secretRef:
                name: {{ include "contest-helper.secretName" . }}
          # 프로세스 생존 확인용 간단 liveness(파이썬 프로세스가 떠 있나).
          livenessProbe:
            exec:
              command: ["sh", "-c", "pgrep -f 'worker.main' > /dev/null"]
            initialDelaySeconds: 20
            periodSeconds: 30
          resources:
            {{- toYaml .Values.resources.worker | nindent 12 }}
